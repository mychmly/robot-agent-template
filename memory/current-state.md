# current-state.md

## 当前状态

本文件已退出当前模板高频持久化主链。

## 当前说明

- 运行态当前默认显示在前端 `Runtime Snapshot` 中
- 只有 `owner-profile.md` 与 `long-term-memory.md` 继续作为右侧 `Persistent Memory` 的持久资产
- 若后续需要重新引入跨轮状态文件，应先明确其是否属于 durable memory，而不是展示镜像
